План:
1) довести до ума определение аккорда (мб попробовать иной подход)
(done 2) фильтровать определяемые частоты (убирать гармоники), выявлять основные частоты (ноты), образующие аккорд
3) разобраться с принципом построения аккорда (минор/мажор)
4) перейти к обработке продолжительных файлов - песен (замедленных)


Финальный продукт:
- получение нот загруженного wav файла - 

1) график частоты от времени, на котором будут отображаться сыгранные в один момент ноты (частоты) и отдельно сыгранные ноты


Доделать:
1) Включить использование ООП (классов): 
класс1 - определятель аккорда (получает спектр - выдает название)
класс2 - фурье ()
2) по-другому определять аккорд
(done 3) исправить функция определения названия аккорда (delta)




Сделано:
1) изменён метод определения аккорда






# 1. Переходим в папку проекта
cd C:\Users\Roman\Desktop\game

# 2. Компилируем и запускаем декодер (создаёт signal.txt)
g++ decoder.cpp -o decoder.exe -lfftw3 -lsndfile
./decoder.exe

# 3. Переходим в папку build
cd build

# 4. Генерируем Makefile
cmake .. -G "MinGW Makefiles"

# 5. Собираем furi.exe
cmake --build . --config Release

# 6. Копируем нужные файлы из game в build
cp ../signal.txt .
cp ../frec_to_note.txt .
cp ../chords.json .

# 7. Запускаем анализ
./furi.exe








cd C:\Users\Roman\Desktop\game; g++ decoder.cpp -o decoder.exe -lfftw3 -lsndfile; ./decoder.exe; cd build; cmake .. -G "MinGW Makefiles"; cmake --build . --config Release; cp ../signal.txt .; cp ../frec_to_note.txt .; cp ../chords.json .; ./furi.exe